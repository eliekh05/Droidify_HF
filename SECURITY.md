# Security

Droidify is read-only. No accounts, no auth, no uploads, no database.

In scope: FastAPI vulnerabilities, SSRF in scrapers, dependency issues.

Out of scope: accuracy of upstream data, content of external download links.

Report via GitHub issue tagged `[security]`. Do not disclose publicly before a fix is deployed.
